---
phase: 06-export
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified: []
autonomous: false
requirements:
  - EXPT-01
  - EXPT-02
  - EXPT-03

must_haves:
  truths:
    - "User can navigate to /export from any page via the nav bar"
    - "All 7 sheets are visible as checkboxes, all checked by default"
    - "Period selector populates from real data"
    - "Preview row counts appear after selecting a period"
    - "Clicking Export downloads a valid .xlsx file"
    - "Downloaded file opens in Excel/LibreOffice with correct sheet names, formatted headers, Rupiah values, and grand totals"
  artifacts:
    - path: "manta-finance/app/export/page.tsx"
      provides: "Accessible /export page"
    - path: "manta-finance/app/export/_components/ExportClient.tsx"
      provides: "Working two-column export UI"
  key_links:
    - from: "Browser nav"
      to: "/export"
      via: "Export link in all page navs"
      pattern: "href=\"/export\""
---

<objective>
Human verification of the complete Phase 6 export flow. Run the dev server, test navigation to /export from other pages, configure an export, download the file, and verify formatting inside the .xlsx file.

Purpose: Confirms the user-facing export feature works end-to-end before marking Phase 6 complete.
Output: Phase 6 human-verified and complete.
</objective>

<execution_context>
@C:/Users/adity/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-export/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite green + dev server ready</name>
  <files></files>
  <action>
    Run the full test suite to confirm no regressions:
    cd "H:/AI/VERA/Manta Racing/manta-finance" && npx vitest run

    Run a production build to confirm no TypeScript or Next.js build errors:
    cd "H:/AI/VERA/Manta Racing/manta-finance" && npm run build

    If either fails, fix the issue before proceeding to the checkpoint. Common issues:
    - Missing ExcelJS import in route.ts (check npm install ran)
    - TypeScript error in ExportClient (check all filter state types)
    - Build error due to ExcelJS requiring Node.js runtime (add export const runtime = 'nodejs' to route.ts if needed)

    Once both pass, start the dev server:
    cd "H:/AI/VERA/Manta Racing/manta-finance" && npm run dev
  </action>
  <verify>
    <automated>cd "H:/AI/VERA/Manta Racing/manta-finance" && npx vitest run && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>All vitest tests pass. npm run build exits 0. Dev server started.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 6 export feature:
    - /export page with two-column layout (config left, preview right)
    - 7-sheet checklist, all selected by default
    - Filters: period, date range, customer, category, payment status
    - Preview panel showing row counts per selected sheet
    - Export button that downloads a formatted .xlsx file
    - Export nav link added to all 6 existing pages
  </what-built>
  <how-to-verify>
    Visit http://localhost:3000 (login if needed).

    Step 1 — Nav link on existing pages:
    From the Dashboard page, verify "Export" appears as the 7th link in the nav bar.
    Click it — should navigate to /export.

    Step 2 — Export page layout:
    Confirm you see a two-column layout:
    - Left: "Period" dropdown, 7 checkboxes (Laporan Keuangan, Transaksi, Detail Item, Piutang, Stock, Margin Produk, Margin Konsumen), filter controls (date range, customer, category, payment status).
    - Right: "Preview Export" panel with sheet names and row counts (or dashes until period selected).

    Step 3 — Period and preview counts:
    Select a period from the dropdown. Verify the right panel updates to show row counts (numbers, not dashes) for each sheet.

    Step 4 — Sheet selection:
    Uncheck "Detail Item" and "Margin Konsumen". Verify they disappear from the preview panel. Re-check them.

    Step 5 — Download all sheets (unfiltered):
    With all sheets selected and no filters (just period selected), click "Export Excel".
    Button should show "Generating..." briefly, then a file named Manta-Racing-Export-{PeriodName}.xlsx should download.

    Step 6 — Open the downloaded file in Excel or LibreOffice:
    Verify:
    a. File opens without error.
    b. 7 sheet tabs are present (Laporan Keuangan, Transaksi, Detail Item, Piutang, Stock, Margin Produk, Margin Konsumen — or Indonesian equivalents).
    c. Each sheet has a header row with dark background color and white text.
    d. Money columns show Rupiah-formatted values (Rp prefix with number formatting).
    e. Transaksi sheet has a TOTAL row at the bottom with bold text.
    f. Column widths are reasonably wide (auto-width applied).

    Step 7 — Test a filter:
    On the export page, type a customer name in the Customer filter field. Click Export.
    Open the file — verify Transaksi sheet only shows that customer's transactions (other sheets unaffected).

    Step 8 — Navigation from export page:
    From /export, verify all 7 nav links work (click Dashboard, click back to Export, etc.).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps passed, or describe which steps failed</resume-signal>
</task>

</tasks>

<verification>
All steps in the checkpoint task above pass.
</verification>

<success_criteria>
Phase 6 complete when:
- All 8 verification steps pass
- Downloaded .xlsx opens with correct formatting
- Nav consistent across all 7 pages
- Phase 6 marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-export/06-04-SUMMARY.md`

Also update .planning/ROADMAP.md: mark Phase 6 complete with today's date.
Update .planning/STATE.md: set status = complete, update progress to 6/6 phases, 100%.
</output>
