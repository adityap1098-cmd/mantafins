---
phase: 05-finance-receivables
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - manta-finance/prisma/schema.prisma
autonomous: true
requirements: [FIN-05]

must_haves:
  truths:
    - "OperationalCost model exists in schema.prisma with periodId, description, amount fields"
    - "Prisma client is regenerated with OperationalCost types"
    - "data/manta.db OperationalCost table exists (prisma db push confirmed)"
  artifacts:
    - path: "manta-finance/prisma/schema.prisma"
      provides: "OperationalCost model scoped to Period"
      contains: "model OperationalCost"
  key_links:
    - from: "manta-finance/prisma/schema.prisma"
      to: "data/manta.db"
      via: "prisma db push"
      pattern: "OperationalCost"
---

<objective>
Add the OperationalCost model to the Prisma schema so the Finance API (plan 05-02) can store and retrieve manual operational cost entries per period.

Purpose: Net profit = gross profit minus operational costs. This requires a dedicated table to persist user-entered costs (rent, salaries, utilities, etc.) per period.
Output: Schema updated, DB migrated, Prisma client types regenerated.
</objective>

<execution_context>
@C:/Users/adity/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@manta-finance/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OperationalCost model to schema.prisma and migrate</name>
  <files>manta-finance/prisma/schema.prisma</files>
  <action>
    Add the following model to schema.prisma below the PaymentLog model:

    ```prisma
    model OperationalCost {
      id          String   @id @default(cuid())
      periodId    String
      period      Period   @relation(fields: [periodId], references: [id])
      description String
      amount      Float
      createdAt   DateTime @default(now())
    }
    ```

    Also add `operationalCosts OperationalCost[]` relation field to the existing Period model.

    Then run from the manta-finance directory:
    1. `npx prisma db push` — creates the OperationalCost table in data/manta.db
    2. `npx prisma generate` — regenerates client types

    Confirm output includes "Your database is now in sync" and no errors.
  </action>
  <verify>
    <automated>cd "H:\AI\VERA\Manta Racing\manta-finance" && npx tsc --noEmit</automated>
  </verify>
  <done>
    schema.prisma contains `model OperationalCost` with periodId, description, amount, createdAt fields.
    Period model has `operationalCosts OperationalCost[]` relation.
    `npx prisma db push` confirmed "Your database is now in sync".
    `tsc --noEmit` passes clean.
  </done>
</task>

</tasks>

<verification>
- `manta-finance/prisma/schema.prisma` contains `model OperationalCost`
- `tsc --noEmit` passes with no errors
- `npx prisma db push` output confirms DB is in sync
</verification>

<success_criteria>
OperationalCost table exists in SQLite. Prisma client has OperationalCost type. Plan 05-02 can import and use `prisma.operationalCost.*` without type errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-finance-receivables/05-01-SUMMARY.md`
</output>
