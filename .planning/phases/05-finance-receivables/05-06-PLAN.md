---
phase: 05-finance-receivables
plan: "06"
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - manta-finance/app/dashboard/page.tsx
  - manta-finance/app/import/page.tsx
  - manta-finance/app/stock/page.tsx
  - manta-finance/app/sales/page.tsx
  - manta-finance/app/finance/page.tsx
  - manta-finance/app/receivables/page.tsx
autonomous: false
requirements: [FIN-01, FIN-02, FIN-03, FIN-04, FIN-05, PIUT-01, PIUT-02, PIUT-03, PIUT-04]

must_haves:
  truths:
    - "All 6 nav pages show a consistent 6-link nav bar: Dashboard, Import Data, Stock, Penjualan, Finance, Piutang"
    - "The active page link is visually distinct (bold + blue underline) on each respective page"
    - "User can verify end-to-end: add operational cost → net profit recalculates; record payment → balance updates"
  artifacts:
    - path: "manta-finance/app/dashboard/page.tsx"
      provides: "Updated 6-link nav"
      contains: "/finance"
    - path: "manta-finance/app/finance/page.tsx"
      provides: "Finance active nav link"
    - path: "manta-finance/app/receivables/page.tsx"
      provides: "Piutang active nav link"
  key_links:
    - from: "All pages nav"
      to: "/finance and /receivables"
      via: "Next.js Link"
      pattern: "href=\"/finance\""
---

<objective>
Update navigation across all existing pages to include Finance and Piutang links, then human-verify the full Phase 5 flow works end-to-end.

Purpose: Nav is currently 4 links; Phase 5 adds 2 new pages that must be reachable from anywhere. Human verification confirms all 5 success criteria are met.
Output: All 6 pages have consistent 6-link nav. Phase 5 verified complete.
</objective>

<execution_context>
@C:/Users/adity/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@manta-finance/app/dashboard/page.tsx
@manta-finance/app/import/page.tsx
@manta-finance/app/stock/page.tsx
@manta-finance/app/sales/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update nav to 6 links across all pages</name>
  <files>
    manta-finance/app/dashboard/page.tsx
    manta-finance/app/import/page.tsx
    manta-finance/app/stock/page.tsx
    manta-finance/app/sales/page.tsx
    manta-finance/app/finance/page.tsx
    manta-finance/app/receivables/page.tsx
  </files>
  <action>
    For each of the 6 pages, update the `<nav>` block to contain exactly these 6 links in order:

    ```tsx
    <nav className="bg-white border-b border-gray-100 px-6 py-2 flex gap-4">
      <Link href="/dashboard" className={...}>Dashboard</Link>
      <Link href="/import" className={...}>Import Data</Link>
      <Link href="/stock" className={...}>Stock</Link>
      <Link href="/sales" className={...}>Penjualan</Link>
      <Link href="/finance" className={...}>Finance</Link>
      <Link href="/receivables" className={...}>Piutang</Link>
    </nav>
    ```

    Active link style: `"text-sm text-blue-600 hover:text-blue-800 font-semibold border-b-2 border-blue-600"`
    Inactive link style: `"text-sm text-gray-600 hover:text-blue-800 font-medium"`

    Finance and Receivables pages already have these links from plans 05-04 and 05-05. For those files, verify the nav is consistent rather than duplicating work.

    For dashboard/page.tsx, import/page.tsx, stock/page.tsx, and sales/page.tsx: add Finance and Piutang links to their existing 4-link navs.

    After all updates, run:
    - `npx tsc --noEmit`
    - `npm run build`
    Both must pass.
  </action>
  <verify>
    <automated>cd "H:\AI\VERA\Manta Racing\manta-finance" && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 6 pages have 6-link nav with correct active styling.
    `tsc --noEmit` and `npm run build` pass with no errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 complete — Finance page (/finance) with P&L report + operational cost management + product margin + customer discount tables. Receivables page (/receivables) with per-customer table, expand-to-see-sales, payment recording, and purchase history. 6-link nav across all pages.
  </what-built>
  <how-to-verify>
    Start the dev server: `cd manta-finance && npm run dev`

    **Finance page (http://localhost:3000/finance):**
    1. Select a period — P&L table should appear with Pendapatan, HPP, Laba Kotor, Biaya Operasional, Laba Bersih rows
    2. Add an operational cost: enter a description (e.g. "Sewa Gudang") and amount (e.g. 500000), click Tambah
    3. Confirm: new cost appears in the list AND Laba Bersih decreases by the entered amount
    4. Delete the cost — confirm Laba Bersih returns to previous value
    5. Scroll down — Product Margin table and Customer Discount table should appear with data
    6. Click a column header in Product Margin table — rows should sort

    **Receivables page (http://localhost:3000/receivables):**
    1. Select a period — customer receivables table should show
    2. Click "Lihat Detail" on a customer with outstanding balance — individual sale rows expand
    3. Click "Catat Bayar" on a sale with balance > 0 — PaymentModal opens
    4. Enter a partial amount (less than full balance), click Simpan Pembayaran
    5. Confirm: modal closes, sale row shows updated balance, customer total outstanding decreases
    6. Click "Riwayat" on a customer — CustomerHistoryPanel opens showing their transaction list
    7. Close the history panel

    **Nav verification:**
    8. Confirm all pages (Dashboard, Import, Stock, Penjualan, Finance, Piutang) show 6-link nav
    9. Each page highlights the correct active link
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 steps pass, or describe which steps failed</resume-signal>
</task>

</tasks>

<verification>
- `tsc --noEmit` passes
- `npm run build` passes
- All 9 human verification steps pass
</verification>

<success_criteria>
All Phase 5 success criteria verified by human:
1. P&L report visible with all financial metrics
2. Operational cost add/delete recalculates net profit
3. Receivables table shows per-customer totals
4. Payment recording updates balance immediately
5. Product margin and customer discount tables visible and sortable
Nav is consistent across all 6 pages. Phase 5 marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-finance-receivables/05-06-SUMMARY.md`
</output>
