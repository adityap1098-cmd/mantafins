---
phase: 02-import-and-calculations
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified: []
autonomous: false
requirements: [IMP-06, CALC-05]

must_haves:
  truths:
    - "User can complete the full upload flow: create period → upload products → upload sales"
    - "After sales upload, database contains Sale and SaleItem rows with HPP values from ProductSnapshot"
    - "Warnings appear for any product names in the Sales Report that have no match in Products"
    - "Historical data from a different period is not affected by uploading to a new period"
    - "All 6 Phase 2 success criteria verified by human"
  artifacts:
    - path: "data/manta.db"
      provides: "SQLite database with populated Period, ProductSnapshot, Sale, SaleItem rows after test upload"
  key_links:
    - from: "/import page"
      to: "data/manta.db"
      via: "full upload flow: create period → products → sales"
      pattern: "POST /api/upload/sales"
---

<objective>
Human verification of the complete Phase 2 upload workflow. The user runs the app, creates a test period, uploads both Excel files, and confirms all 6 success criteria are met.

Purpose: Phase 2 is only complete when the core value ("upload 2 Excel → all metrics auto-computed") is verified end-to-end by a human.
Output: Phase 2 marked complete, STATE.md updated.
</objective>

<execution_context>
@C:/Users/adity/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and prepare for verification</name>
  <files></files>
  <action>
Run the dev server and confirm no runtime errors:

```bash
cd "H:/AI/VERA/Manta Racing/manta-finance" && npm run dev
```

Confirm the server starts at localhost:3000 without errors. If there are any TypeScript or module resolution errors in the terminal, fix them before proceeding to the checkpoint.

Check that the /import route is accessible (200 response when logged in).
  </action>
  <verify>
    <automated>cd "H:/AI/VERA/Manta Racing/manta-finance" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build passes — no build errors, all routes compile</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 2 import and calculation system:
- Period management (create + select)
- Products Excel upload → ProductSnapshot rows in DB
- Sales Report Excel upload → Sale + SaleItem rows with HPP computed from product matches
- Warning display for unmatched product names
- /import page accessible from dashboard nav
  </what-built>
  <how-to-verify>
1. Visit http://localhost:3000 — confirm redirect to login if not logged in
2. Log in with password (from .env APP_PASSWORD)
3. Click "Import Data" link in dashboard — confirm /import page loads
4. Create a new period: enter name "Maret 2026", month 3, year 2026 → click "Buat Periode"
   - Confirm: period appears in the dropdown and is auto-selected
5. With period selected, upload a Products Excel file (.xlsx with columns: Nama, Code, Kode Kategori, Biaya, Harga, Kuantitas)
   - Confirm: success message shows "Berhasil import X produk"
   - Confirm: if using a test file with 1-2 rows, X matches row count
6. Upload a Sales Report Excel file (.xlsx with columns: Tanggal, No Referensi, Pemasok, Konsumen, Produk (Qty), Grand Total, Terbayar, Balance, Status Terbayar)
   - Confirm: success message shows "Berhasil import X transaksi"
   - If product names in Sales Report don't match Products file: confirm yellow warning box appears listing unmatched names
7. Create a second period "April 2026" and upload different data — confirm Maret 2026 data is unaffected
8. Verify upload buttons are disabled (greyed out, "Pilih periode terlebih dahulu") before selecting a period

Phase 2 success criteria check:
- [ ] Can create new period
- [ ] Can upload Products Excel → ProductSnapshot saved
- [ ] Can upload Sales Report Excel → Sale + SaleItem saved
- [ ] HPP, margin metrics auto-computed (visible in SaleItem rows in DB)
- [ ] Warnings shown for unmatched products
- [ ] Historical period data unchanged when uploading to new period
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 criteria pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 6 Phase 2 success criteria from ROADMAP.md verified:
1. User can create a new monthly period
2. User can upload Products Excel → ProductSnapshot saved with HPP
3. User can upload Sales Report → transactions parsed and matched to products
4. After upload, HPP, discount, gross profit, margin auto-calculated for every transaction
5. User sees clear upload status: success, warnings for unmatched products, or errors
6. Historical period data does not change when new period is uploaded
</verification>

<success_criteria>
Human approves all 6 Phase 2 success criteria. Phase 2 marked complete in STATE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/02-import-and-calculations/02-06-SUMMARY.md`
Update STATE.md: Phase 2 complete, progress 33% (2 of 6 phases), next = Phase 3 Dashboard
</output>
