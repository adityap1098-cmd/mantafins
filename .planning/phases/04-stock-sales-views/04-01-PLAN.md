---
phase: 04-stock-sales-views
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - manta-finance/app/api/stock/route.ts
  - manta-finance/app/api/stock/[sku]/route.ts
autonomous: true
requirements: [STOK-01, STOK-06]

must_haves:
  truths:
    - "GET /api/stock?periodId=X returns array of stock rows with sku, name, category, hpp, hargaJual, stock, marginUnit, marginPersen"
    - "GET /api/stock?periodId=X returns an inventorySummary object with totalHppValue, totalHargaJualValue, potentialProfit"
    - "PATCH /api/stock/[sku]?periodId=X with {stock: N} updates the ProductSnapshot.stock field and returns the updated row"
    - "Both routes return 401 if session cookie is absent or invalid"
  artifacts:
    - path: "manta-finance/app/api/stock/route.ts"
      provides: "GET handler for stock list + inventory summary"
      exports: [GET]
    - path: "manta-finance/app/api/stock/[sku]/route.ts"
      provides: "PATCH handler for inline stock quantity edit"
      exports: [PATCH]
  key_links:
    - from: "manta-finance/app/api/stock/route.ts"
      to: "prisma.productSnapshot"
      via: "findMany where periodId"
      pattern: "prisma\\.productSnapshot\\.findMany"
    - from: "manta-finance/app/api/stock/[sku]/route.ts"
      to: "prisma.productSnapshot"
      via: "update where periodId+sku"
      pattern: "prisma\\.productSnapshot\\.update"
---

<objective>
Create two API routes that power the stock management view.

Purpose: The stock UI (Plan 02) needs data endpoints before it can be built. These routes derive margin metrics from stored HPP/hargaJual and compute an inventory value summary.
Output: GET /api/stock and PATCH /api/stock/[sku] — both session-protected.
</objective>

<execution_context>
@C:/Users/adity/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@manta-finance/lib/prisma.ts
@manta-finance/lib/auth.ts
@manta-finance/app/api/dashboard/route.ts

<interfaces>
<!-- Key types available in the codebase. Executor uses these directly. -->

From prisma/schema.prisma — ProductSnapshot model:
```
model ProductSnapshot {
  id        String  @id @default(cuid())
  periodId  String
  period    Period  @relation(fields: [periodId], references: [id])
  sku       String
  name      String
  category  String
  hpp       Float
  hargaJual Float
  stock     Int

  @@unique([periodId, sku])
}
```

Auth pattern (from existing routes):
```typescript
import { validateSession, SESSION_COOKIE_NAME } from '@/lib/auth'
const token = req.cookies.get(SESSION_COOKIE_NAME)?.value
if (!token) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
const valid = await validateSession(token)
if (!valid) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GET /api/stock route</name>
  <files>manta-finance/app/api/stock/route.ts</files>
  <behavior>
    - Returns 401 when session cookie is missing
    - Returns 400 when periodId query param is missing
    - Returns array of StockRow objects when periodId is valid
    - Each StockRow has: id, sku, name, category, hpp, hargaJual, stock, marginUnit (hargaJual - hpp), marginPersen ((hargaJual - hpp) / hargaJual * 100)
    - Returns inventorySummary: { totalHppValue: SUM(hpp*stock), totalHargaJualValue: SUM(hargaJual*stock), potentialProfit: totalHargaJualValue - totalHppValue }
  </behavior>
  <action>
    Create `manta-finance/app/api/stock/route.ts`:

    1. Auth guard using SESSION_COOKIE_NAME cookie + validateSession (same pattern as /api/dashboard/route.ts).
    2. Read `periodId` from searchParams — return 400 if missing.
    3. Query: `prisma.productSnapshot.findMany({ where: { periodId } })`.
    4. Map each snapshot to StockRow: spread all snapshot fields plus computed `marginUnit = hargaJual - hpp` and `marginPersen = hpp > 0 ? ((hargaJual - hpp) / hargaJual) * 100 : 0`.
    5. Compute inventorySummary: reduce over rows to get totalHppValue, totalHargaJualValue, potentialProfit.
    6. Return `{ rows: StockRow[], inventorySummary }`.

    Export types `StockRow` and `InventorySummary` from this file — Plan 02 UI imports them.
  </action>
  <verify>
    <automated>cd "H:/AI/VERA/Manta Racing/manta-finance" && npx tsc --noEmit</automated>
  </verify>
  <done>GET /api/stock?periodId=X returns { rows: [...], inventorySummary: {...} }; missing periodId returns 400; no session returns 401. tsc --noEmit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create PATCH /api/stock/[sku] route</name>
  <files>manta-finance/app/api/stock/[sku]/route.ts</files>
  <action>
    Create `manta-finance/app/api/stock/[sku]/route.ts`:

    1. Auth guard (same pattern — SESSION_COOKIE_NAME + validateSession).
    2. Read `periodId` from searchParams — return 400 if missing.
    3. Read `sku` from `params.sku` (Next.js dynamic segment).
    4. Parse request body: `const body = await req.json()`. Validate `typeof body.stock === 'number' && Number.isInteger(body.stock) && body.stock >= 0` — return 400 with `{ error: 'stock must be a non-negative integer' }` if invalid.
    5. Update: `prisma.productSnapshot.update({ where: { periodId_sku: { periodId, sku } }, data: { stock: body.stock } })`.
    6. Recompute marginUnit and marginPersen from the updated row, return `{ row: StockRow }` with updated values.
    7. Handle Prisma not-found (P2025) — return 404 `{ error: 'Product not found' }`.
  </action>
  <verify>
    <automated>cd "H:/AI/VERA/Manta Racing/manta-finance" && npx tsc --noEmit</automated>
  </verify>
  <done>PATCH /api/stock/[sku]?periodId=X with { stock: N } returns updated StockRow; invalid body returns 400; not found returns 404; no session returns 401. tsc --noEmit passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` completes successfully
- Both route files exist at the correct paths
- StockRow and InventorySummary types exported from GET route for Plan 02 consumption
</verification>

<success_criteria>
Two session-protected API routes exist:
1. GET /api/stock?periodId=X → { rows: StockRow[], inventorySummary: InventorySummary }
2. PATCH /api/stock/[sku]?periodId=X + body { stock: N } → { row: StockRow }
Both return 401 without valid session, 400 for missing required params.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stock-sales-views/04-01-SUMMARY.md`
</output>
